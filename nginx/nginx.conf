events {}

http {
	include mime.types;
	server {
		listen 80;
		root /site;

		try_files $uri /cat.png /greet /not_found;
		location /not_found {
			return 404 "does not exist";
		}
		location /greet {
			return 200 "$host\n$uri\n$arg_name";
		}
	}
}