events {}

http {
        server {
                listen 80;
                server_name 192.168.1.107;
                root /usr/share/nginx/html;
                index index.html;
                location / {
                        try_files $uri $uri/ =404;
                }
        }
}